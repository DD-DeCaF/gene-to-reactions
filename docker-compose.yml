web:
  build: .
  dockerfile: Dockerfile
  links:
    - redis
  ports:
    - 8000:8000
    - 50053:50053
  environment:
    - PYTHONUNBUFFERED=1
    - SETTINGS_OBJECT=genotype_to_model.settings.Default
    - GENOMICS_API
redis:
  image: redis
  ports:
    - 6379:6379
